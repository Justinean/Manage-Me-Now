   {{!-- <div class="container">
    <div class="row mt-5 pt-3">
  </div>
  <!-- User can select a project -->
  <div class="row">
  <h2 class="mb-4">My Projects</h2>
     <div class="btn col-sm bg-light border border-dark rounded my-2 mx-2 p-2 text-center">PROJECT NAME {{#if isMgr<!-- match the "is manager" syntax to back end -->}}
      <i class="btn fas fa-trash-alt" id="deleteProjectTrash"></i>
       {{/if}}</div>
    </div>
    <div class="row">
      <h2 class="mb-3 mt-4">Project Name</h2>
        <!-- employees div -->
         <div class="col-sm col-4 bg-light border border-dark rounded my-2 mx-2 p-2 text-center">
           <h4 class="mt-3 mb-1">Manager</h4>
           <ul class="list-group list-group-vertical">
            <li class="list-group-item my-1">Person 1</li>
            </ul>
           <h4 class="mt-3 mb-1">Team</h4>
           <ul class="list-group list-group-vertical">
           <li class="list-group-item my-1">Person 2 {{#if isMgr<!-- match the "is manager" syntax to back end -->}}
           <i class="btn bi bi-dash-circle" id="deleteEmployeeMinus"></i>
            {{/if}}</li>
          </ul>
         </div>
         <!-- to do div -->
         <div class="col-sm bg-light border border-dark rounded my-2 mx-2 p-2">
          <h4 class="mt-3 mb-1 text-center">Tasks</h4>
           <ul class="list-group list-group-vertical">
            <li class="list-group-item my-1"> Task 1 {{#if isMgr <!-- match the "is manager" syntax to back end -->}} <i class="btn fas fa-trash-alt" id="deleteTaskTrash"></i>{{/if}}</li>
          </ul>
          </div>
         <!-- close row -->
         </div>
  <!-- close container -->
  </div> --}}

  <section>
  <h2>Welcome, {{projects.username}}</h2>
  <div>
    <h3>Your projects</h3>
    {{#if projects.length}}
    {{#each projects as |project|}}
    {{> project-details}}
    {{/each}}
    {{else}}
    <p>Get those fingers clicking!</p>
    {{/if}}
  </div>
  <div>
    <a href="/dashboard/newProject"><button>Add New project!</button></a>
  </div>
</section>