{{!-- assigned to --}}
<div class="my-3 rounded list-group-item container">
  <div class="row">
    <h3 class="text-start col-10 my-3">Assigned to {{task_user}}</h3>
    {{#if @root.projectPage}}
    {{#if @root.isMgr}}
    <div class="col-2 px-1 text-end">
      <i class="mt-3 btn fas fa-trash-alt" id="deleteTaskBtn" data-id="{{id}}"></i>
    </div>
    {{/if}}
    {{/if}}
    <hr>
    {{!-- task name --}}
    <div class="text-start">{{name}}</div>
  </div>
</div>